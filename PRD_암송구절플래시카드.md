# 제자반 암송구절 플래시카드 앱 PRD

앱 이름: 제자반 암송구절  
작성일: 2026-08-23  
작성자: 수진

---

## 1. 개요

제자반 수강생이 암송구절을 효과적으로 외울 수 있도록 돕는 플래시카드 앱이다. 총 3권, 64구절의 암송구절이 미리 내장되어 있으며, 앞면(제목/번호) → 뒷면(구절 내용) 방식과 두문(첫 글자) 힌트 방식을 조합해 단계적으로 암송을 훈련할 수 있게 한다.

---

## 2. 타겟 사용자

제자반 수강생. 별도 관리자 기능 없이, 개인이 독립적으로 암송 훈련에 사용한다.

---

## 3. 핵심 가치 제안

- 별도 설치 없이 모바일 브라우저에서 바로 사용 가능
- 구절 데이터가 앱 안에 완전히 내장되어 있어 인터넷 연결 없이도 학습 가능
- 앞면/뒷면 플래시카드 + 두문 힌트 방식으로 단계적 암송 훈련 지원
- 마지막으로 본 구절 위치를 자동으로 기억해 이어서 학습 가능

---

## 4. 콘텐츠 구조

### 4.1 전체 구성

| 항목 | 내용 |
|---|---|
| 총 권 수 | 3권 |
| 총 구절 수 | 64구절 |
| 구절 묶음 | 1과당 2구절, 과마다 제목 있음 |

### 4.2 데이터 구조 (예시)

```
권 1
  └ 1과: [과 제목]
      └ 구절 1-1: [구절 내용]
      └ 구절 1-2: [구절 내용]
  └ 2과: [과 제목]
      ...
권 2
  ...
권 3
  ...
```

### 4.3 데이터 소스

구절 데이터는 이미지 또는 PDF 파일로 제공받아, 앱 내부에 정적으로 하드코딩하여 내장한다. 외부 API나 서버 연동 없음.

---

## 5. 주요 기능

### 5.1 시작 화면 (진입 전)

앱 최초 진입 시 목차 화면으로 바로 넘어가지 않고, 시작 화면을 먼저 보여준다.

**구성 요소**
- 앱 이름 "제자반 암송구절" 중앙 표시 (로고/타이틀)
- "학습 시작" 버튼 — 탭하면 목차 화면으로 이동
- 이전에 학습 기록이 있는 경우: "이어서 학습하기" 버튼도 함께 표시 (마지막 위치로 바로 이동)
- 첫 방문 시(학습 기록 없음): "학습 시작" 버튼만 표시

**동작 방식**
- 자동 전환 없음 — 사용자가 직접 버튼을 탭해서 진입
- 시작 화면은 앱 최초 로딩 시에만 표시되며, 학습 중 뒤로가기 시에는 목차 화면으로 이동

### 5.2 목차 (네비게이션)

- 시작 화면에서 진입 후 **권 단위 탭**으로 1권 / 2권 / 3권 구분
- 각 권 아래에 해당 권의 **과 목록**을 세부 목록으로 표시
- 목록에서 원하는 과를 선택하면 해당 과의 첫 구절로 이동
- 상단 고정 배너: 학습 기록이 있는 경우 "이어서 학습하기" 버튼 표시 (목차 위에 고정)

### 5.3 플래시카드 학습

**앞면**
- 권 번호, 과 번호, 과 제목, 구절 번호 표시
- "탭해서 뒤집기" 안내 문구

**뒷면**
- 상단: 권 번호, 과 번호, 과 제목 표시
- 본문: 구절 전체 내용 표시

**두문 모드**
- 뒷면 구절 내용을 각 단어의 첫 글자만 표시
- 앞면 또는 뒷면에서 별도 토글로 두문 모드 ON/OFF 전환 가능

### 5.4 카드 탐색

- **순서 모드**: 현재 과 내 구절 순서대로 앞/뒤 이동 (← →)
- **랜덤 모드**: 셔플 범위를 선택해 랜덤으로 카드 제공
  - 범위 옵션 (4가지):
    1. **현재 권 내** (기본값)
    2. **전체 64구절**
    3. **모르는 구절만** (체크된 구절 대상)
    4. **말씀구절 선택** — 사용자가 특정 구절들을 직접 골라 셔플 범위로 지정
  - 범위 선택 UI는 랜덤 모드 진입 시 또는 별도 설정 버튼으로 접근
- 화면 하단 또는 상단에 순서/랜덤 모드 전환 버튼 제공

### 5.5 모르는 구절 체크

- 카드 학습 중 "모르는 구절"로 체크하는 버튼 제공 (구절당 토글)
- 체크 상태는 **localStorage**에 저장되어 앱 재진입 후에도 유지
- 체크 범위: 전체 구절 개별 체크 가능 (권 단위 일괄 해제 기능 제공)
- 랜덤 모드에서 "체크된 구절만" 범위 선택 시, 체크된 구절만 셔플하여 집중 연습 가능
- 목차 화면에서 각 구절/과 옆에 체크 여부 표시 (몇 개 체크됐는지 한눈에 파악)

### 5.6 학습 위치 저장

- 마지막으로 본 구절(권·과·구절 번호)을 **localStorage**에 자동 저장
- 앱 재진입 시 "이어서 학습하기" 옵션으로 마지막 위치로 바로 이동 가능
- 체크 데이터(모르는 구절 목록)도 함께 localStorage에 저장

---

## 6. 플랫폼 및 기술

| 항목 | 내용 |
|---|---|
| 플랫폼 | 모바일 웹 위주, 반응형 (데스크탑도 지원) |
| 기술 스택 | HTML / CSS / Vanilla JS (또는 React) |
| 데이터 저장 | localStorage (학습 위치 저장) |
| 오프라인 | 정적 내장 데이터로 인터넷 없이도 동작 |

---

## 7. 디자인 방향

- **무드**: 깔끔하고 미니멀
- **컬러**: 화이트/라이트 그레이 배경, 포인트 컬러 1~2개 절제해서 사용
- **타이포그래피**: 가독성 중심, 구절 본문은 충분한 폰트 크기와 행간 확보
- **UI 컴포넌트**: 불필요한 장식 없이 여백과 타이포그래피로 구분감 표현
- **카드 플립**: 자연스러운 flip 애니메이션

---

## 8. MVP 범위

### MVP 구현 순서

구현은 아래 순서로 진행한다.

**1단계 — 시작 화면 (랜딩)**
- 앱 이름 "제자반 암송구절" 중앙 표시
- "학습 시작" 버튼 (첫 방문 시)
- "이어서 학습하기" 버튼 (학습 기록 있을 때만 표시)

**2단계 — 목차 화면**
- 권별 탭(1권/2권/3권) + 과 세부 목록
- 구절별 체크 여부 표시
- 상단 고정 "이어서 학습하기" 배너 (학습 기록 있을 때)

**3단계 — 플래시카드 학습**
- 앞면(제목/번호) ↔ 뒷면(구절 내용 + 과 정보) 플립
- 두문 모드 토글
- 순서 모드 + 랜덤 모드 전환 (범위: 현재 권(기본) / 전체 64구절 / 모르는 구절만 / 말씀구절 선택)
- 모르는 구절 체크 기능 + 권 단위 일괄 해제
- 마지막 위치 저장, 체크 데이터 localStorage 영구 저장

### Phase 2 (이후 추가 검토)
- 학습 통계 (체크된 구절 수, 권별 진도율 등)
- PWA 지원 (홈 화면 추가, 완전한 오프라인 동작)

---

## 9. 성공 지표

- 실제 제자반 수강생이 앱을 통해 암송 훈련을 지속적으로 활용하는지 여부
- (정량 지표는 사용자 확보 후 별도 논의)

---

## 10. 향후 논의 필요 사항 (Open Questions)

- 실제 구절 데이터 (각 권·과 제목, 구절 내용 전체) — 이미지/PDF 제공 후 입력 및 검수
- 두문 모드에서 조사/접속사 처리 방식 (첫 글자 포함할지, 제외할지)
- 말씀구절 선택 모드의 UI 방식 (목차에서 체크박스로 고르는 방식 vs 별도 선택 화면)
- 권 단위 체크 일괄 해제 외, 전체 일괄 해제 버튼도 필요한지 여부
