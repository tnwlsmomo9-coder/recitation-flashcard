# 제자반 암송구절 플래시카드 앱 DESIGN.md

작성일: 2026-08-23
작성자: 수진
관련 문서: PRD_암송구절플래시카드.md

---

## 1. 디자인 방향 요약

군더더기 없이 콘텐츠(구절) 중심의 깔끔하고 미니멀한 무드를 기본으로 한다. 화이트/라이트 그레이 배경 위에 소프트 인디고/퍼플 포인트 컬러로 영적이고 차분한 분위기를 연출한다. MoMoney에서 참고한 flat design 색면 구성과 넉넉한 여백감을 가져와, 구절 텍스트가 가장 돋보이는 화면을 만든다.

---

## 2. 무드 (Mood)

**깔끔하고 미니멀한, 집중을 돕는 공간**

- 암송이라는 행위 자체가 집중과 반복을 요구하기 때문에, UI가 방해 요소가 되어서는 안 된다.
- 장식적 요소를 철저히 배제하고, 여백과 타이포그래피만으로 화면을 구성한다.
- 단, 완전히 차갑거나 딱딱한 느낌이 아니라, 소프트 인디고/퍼플 포인트로 영적이고 따뜻한 차분함을 유지한다.

---

## 3. 컬러 (Color)

| 용도 | 색상 | HEX |
|---|---|---|
| 배경 (기본) | 웜 크림 | `#F5F3E8` |
| 배경 (카드/섹션) | 샌드 | `#E7DFC4` |
| 포인트 컬러 (메인) | 비비드 오렌지 | `#FF7C43` |
| 포인트 컬러 (메인, 호버) | 딥 오렌지 | `#E85F27` |
| 포인트 컬러 (서브) | 소프트 피치 | `#FFC9A8` |
| 텍스트 (기본) | 웜 블랙 | `#1E1F19` |
| 텍스트 (보조) | 웜 그레이브라운 | `#7A7462` |
| 보더 | 샌드 라인 | `#DDD5BC` |
| 체크/강조 배지 | 피치 틴트 | `#FFE9DC` |

**컬러 사용 원칙**
- 메인 오렌지(`#FF7C43`)는 CTA 버튼, 현재 선택된 탭, 체크 아이콘 등 핵심 인터랙션 요소에 사용한다.
- 서브 피치(`#FFC9A8`)는 두문 모드 토글 ON 상태, 뱃지, 하이라이트 등 보조 강조에 사용한다.
- 배경은 순백/쿨그레이가 아닌 웜 크림(`#F5F3E8`)으로, 오렌지 포인트와 자연스럽게 어울리도록 한다.
- 카드·버튼·탭·모달 등 UI 형태는 넉넉하게 둥근 라운드(14~24px, 원형 버튼 포함)를 적용해 부드럽고 대담한 인상을 준다.
- 주요 CTA는 호버 시 살짝 떠오르는 인터랙션(translateY + 오렌지 글로우)을 적용해 평면적이지 않은 생동감을 준다. 단, 평상시(resting state)는 색면 위주로 절제한다.
- 시작 화면은 딥 블랙(`#1E1F19` 계열) 배경 위 오렌지/크림 그라디언트 텍스트로 강한 대비를 주고, 목차·카드 등 학습 화면은 가독성을 위해 라이트 웜 크림/샌드를 유지한다.

---

## 4. 타이포그래피 (Typography)

- **메인 폰트**: Pretendard (한국 웹 가독성 최적화)
- **영문 병기 / 숫자**: Pretendard의 영문 글리프 그대로 사용 (별도 영문 폰트 불필요)
- **시작 화면 영문 타이틀 전용**: Jost (지오메트릭 산세리프, Bold) — 에이전시 포스터 톤의 임팩트 있는 대문자 타이틀에만 한정 사용

### 타입 스케일

| 역할 | 크기 | 굵기 | 용도 |
|---|---|---|---|
| Display | 28px | ExtraBold (800) | 카드 앞면 과 제목 |
| Heading | 20px | Bold (700) | 목차 권 제목, 섹션 헤더 |
| Body Large | 18px | Regular (400) | 카드 뒷면 구절 본문 |
| Body | 16px | Regular (400) | 목차 과 목록, 일반 UI |
| Caption | 13px | Medium (500) | 권/과/구절 번호, 보조 레이블 |

**타이포그래피 원칙**
- 구절 본문(Body Large)은 행간 1.8 이상으로 넉넉하게 확보해 암송 시 읽기 편하도록 한다.
- 과 제목(Display)은 ExtraBold로 크게 잡아 카드 앞면에서 임팩트를 준다.
- 권/과 번호 같은 메타 정보는 Caption 크기에 인디고 컬러로 표시해 본문과 시각적으로 분리한다.

---

## 5. UI 컴포넌트 스타일

### 플래시카드
- flat design 기반: 그림자 없이 `#E2E2EE` 보더 또는 `#F0F0F5` 색면으로 카드 영역 구분
- 앞면: 오프 화이트 배경, 중앙 정렬, 과 제목을 Display 사이즈로 크게 배치
- 뒷면: 라이트 그레이(`#F0F0F5`) 배경으로 앞면과 색면 구분, 구절 본문은 좌측 정렬 + 넉넉한 행간
- 카드 플립: 자연스러운 3D flip 애니메이션 (Y축 회전, 0.4s ease)

### 두문 모드
- 뒷면 구절 본문 위에 토글 버튼으로 ON/OFF
- ON 상태: 버튼 배경 라벤더(`#C4C4F0`), 구절 텍스트는 각 단어 첫 글자만 인디고(`#5B5BD6`)로 표시
- OFF 상태: 버튼 배경 보더 그레이, 구절 전체 텍스트 표시

### 탭 (권 선택)
- 1권 / 2권 / 3권 탭
- 선택된 탭: 인디고(`#5B5BD6`) 하단 보더 2px + 텍스트 인디고
- 비선택 탭: 보조 텍스트 컬러(`#6B6B8A`)

### 목차 리스트
- 과 항목: 좌측 과 번호(Caption, 인디고) + 과 제목(Body) + 우측 체크 카운터(예: 1/2)
- 체크된 구절 수는 인디고 틴트 배지(`#EDEDFA`)로 표시
- 구분선은 `#E2E2EE` 1px 보더로 미니멀하게 처리

### 버튼
- 주요 CTA(랜덤 시작, 이어서 학습하기): 인디고(`#5B5BD6`) 배경 + 화이트 텍스트, 라운드 8px
- 보조 버튼(순서/랜덤 전환, 두문 토글): 아웃라인 스타일 또는 틴트 배경
- 호버/탭 시: 살짝 어두워지는 트랜지션 (0.2s ease)

### 모르는 구절 체크 버튼
- 카드 하단 고정 위치
- 체크 전: 보더 아웃라인 스타일
- 체크 후: 인디고 배경 + 화이트 아이콘 (체크마크)

---

## 6. 레이아웃 원칙

- **여백**: MoMoney의 넉넉한 여백감을 참고. 모바일 기준 좌우 패딩 20px 이상, 섹션 간 간격 충분히 확보
- **정렬**: 카드 앞면은 중앙 정렬, 카드 뒷면 본문은 좌측 정렬 (긴 구절 가독성을 위해)
- **flat design**: 불필요한 그라디언트, 그림자, 장식적 일러스트 사용 금지. 색면과 보더로만 구분
- **모바일 우선**: 카드 높이는 화면의 60~70%를 차지해 텍스트가 충분히 크게 보이도록 설계

---

## 7. 레퍼런스 (Reference)

**MoMoney — The Interactive Museum of Money**
https://www.museumofmoney.com/

- 참고 포인트: flat design 기반의 선명한 색면 구성, 넉넉한 여백과 공간감 있는 레이아웃
- CSS Design Awards 태그: animated · colorful · flat design / 교육 카테고리
- 이 앱에 적용 방식: MoMoney의 컬러풀함은 차분한 인디고/라벤더 팔레트로 절제하되, flat design 색면 구성과 여백감 원칙은 그대로 가져온다.
