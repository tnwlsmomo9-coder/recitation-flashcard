## HANDOFF — 현재 저장소 상태

이 문서는 과거 작업 기록이 아니라 다음 작업자가 현재 구현과 유지해야 할 경계를 확인하기 위한 문서입니다. 변경 이력은 Git에서 확인하고, 여기에는 현재 저장소 상태만 남기세요.

### 1) 현재 Git 상태

- 브랜치: `main`
- 로컬 HEAD와 `origin/main`은 이 문서 커밋 기준으로 동일함
- staged 파일: 없음
- unstaged 파일: `css/style.css`
- untracked 파일: 없음
- `css/style.css`의 미커밋 변경은 목록 말씀 주소 `18px`와 필사 이전/다음 `margin-top: 0`에 관한 현재 구현이며, 문서 갱신 과정에서 삭제하거나 되돌리지 않음

### 2) 현재 구현 완료된 기능

#### 앱 및 PWA

- 빌드 과정이 없는 정적 HTML/CSS/JavaScript 앱이며 화면은 시작(`screen-start`), 말씀 목록(`screen-toc`), 학습·암송(`screen-card`), 필사(`screen-writing`)로 구성됨
- `index.html`에 `manifest.json`, `theme-color`, Apple Web App 메타 태그와 홈 화면 아이콘 연결이 존재함
- `index.html`의 load 이벤트에서 `navigator.serviceWorker.register("sw.js")`를 실행함
- `manifest.json`은 standalone 표시, 아이보리 배경색, 플럼 theme color, 192/512px 아이콘을 정의함
- `sw.js`는 `srp-offline-v4` 캐시를 사용하고 동일 출처 GET 요청을 기존 network-first 전략으로 처리하며, 오프라인 탐색 요청은 캐시된 `index.html`로 대체함
- 앱 실행 시 랜딩 이전에 WebView/브라우저의 검은 빈 화면이 길게 노출되지 않도록 `index.html`의 `<head>`에 `html, body { margin: 0; background: #F8F5EE; }`를 인라인으로 선언함. 외부 스타일과 앱 모듈이 준비되기 전부터 아이보리 배경이 표시됨
- GSAP 외부 CDN 의존성을 제거하고 `js/vendor/gsap.min.js`의 GSAP 3.15.0을 `<head>`에서 앱 모듈보다 먼저 동기 로드함. 실행 순서는 `GSAP → js/app.js → initStartHero()`임
- 실제 코드에서 사용되지 않던 SplitText 스크립트와 의존성은 제거되어 있으며 현재 저장소에 SplitText 참조가 없음
- Pretendard와 Black Han Sans의 Google Fonts/jsDelivr 의존성을 제거함. `fonts/PretendardVariable.woff2`를 기존 `Pretendard` family 이름과 `400–800` 굵기 범위로 사용하고, 제목은 `fonts/BlackHanSans-Regular.ttf`를 기존 `Black Han Sans` family 이름과 400 굵기로 사용함
- 두 로컬 폰트의 OFL 고지는 `fonts/LICENSE-Pretendard.txt`, `fonts/LICENSE-Black-Han-Sans.txt`에 포함됨
- `sw.js`의 precache에는 앱 shell과 함께 로컬 GSAP, Pretendard, Black Han Sans가 포함됨. 한 번 온라인에서 최신 서비스 워커와 리소스를 설치·캐시한 뒤에는 완전 오프라인 재실행에서도 랜딩 애니메이션과 학습 화면을 사용할 수 있음. 서비스 워커가 설치되지 않은 최초 방문 자체가 오프라인인 경우는 지원하지 않음
- 시작 화면은 클릭뿐 아니라 `tabindex="0"`, `role="button"`, `aria-label` 및 Enter/Space 키 입력으로 진입 가능함
- 오프라인에서도 아이보리 초기 배경, 보라색 오버레이, 배경 말씀 애니메이션, 엠블럼 등장, 제목 효과, Pretendard/Black Han Sans와 랜딩 이후 학습 화면 진입이 유지되어야 함
- 랜딩의 기존 `js/startHero.js` 애니메이션 로직, UI 구조, 화면 전환과 서비스 워커의 network-first 전략은 변경되지 않음

#### 말씀 목록

- 홈 아이콘과 `신천중부교회 · 제자훈련반` 브랜드 버튼, `전체 / 1권 / 2권 / 3권` 권 탭, 검색, 랜덤 암송 진입, 암송 상태 필터가 구현됨
- 선택된 권을 처음 누르거나 다른 권을 누르면 상태 필터가 펼쳐지고, 이미 선택된 같은 권을 다시 누르면 선택 권과 필터 값은 유지한 채 필터 UI만 접히거나 다시 펼쳐짐
- 검색어가 있으면 선택된 권과 관계없이 전체 권에서 과 번호·과 제목·말씀 주소·본문을 검색함
- 검색 입력과 `랜덤 암송 ›`은 하나의 외곽 컨테이너와 내부 세로 구분선을 공유함
- 상태 필터는 `○ 더 익히기 / ◐ 부분 암기 / ✓ 암기 완료`이며 권 필터와 함께 적용됨. 활성 상태를 다시 누르면 상태 필터가 해제됨
- 각 과는 radius와 그림자 없는 직선형 섹션이며, 라벤더 제목 헤더 전체가 해당 과 전체 학습 버튼으로 동작함
- 과 아래 두 말씀은 모든 화면 크기에서 2열 독립 버튼으로 표시됨. 화면용 `❶/❷` 표시는 CSS에서 숨겨져 있고 데이터 순서와 클릭 로직은 유지됨
- 말씀 주소는 18px, `font-weight: 500`, 한 줄 표시이며 말줄임표를 사용하지 않음. 상태 문구는 그 아래 작은 글씨로 표시됨
- 마지막으로 보던 말씀은 목록에서 옅은 배경으로 구분되고, 목차로 돌아올 때 저장한 스크롤 위치를 복원함

#### 말씀익히기

- 일반 학습 상단에는 `‹ 목차`와 `말씀익히기 | 필사하기` 세그먼트가 표시되며 현재 말씀을 유지한 채 필사로 전환함
- 학습 모드는 `전체 / 나눠보기 / 빈칸암기 / 첫 글자` 네 가지이며 기존 `js/practice.js`, `js/initials.js` 로직을 사용함
- 전체 본문의 각 어절 첫 글자는 기존 판별 방식 그대로 `.first-char`로 감싸고 `font-weight: 600`과 플럼 계열 색상으로 표시함
- 첫 글자 모드의 `확인하기`는 compact 버튼이며, 확인 후 전체 본문과 암송 상태 기록 흐름으로 연결됨
- 카드 하단에는 `더 익히기 / 부분 암기 / 암기 완료` footer가 붙어 있음. 항목 사이 구분선, 선택 상태 라벤더 배경, 최소 44px 터치 높이를 사용함
- 카드 상단 암송 상태 배지는 저장된 현재 상태를 표시하는 역할만 함
- 하단 상태 선택 영역에는 레이아웃 높이를 늘리지 않는 absolute overlay 방식으로 `현재 암송 상태 ⓘ` 가이드가 배치됨. 아이콘을 누르면 화면 경계 안의 작은 팝오버로 `더 익히기 — 본문 없이 암송이 어려움 / 부분 암기 — 중간중간 도움이 필요함 / 암기 완료 — 본문 없이 끝까지 가능` 기준을 확인할 수 있음
- 가이드의 세 기준은 각각 한 줄로 표시되고 팝오버 내부에는 가로·세로 스크롤이 없음. 아이콘 재클릭, 바깥 영역 클릭 또는 Escape 키로 닫힘
- 암송 상태를 선택하면 저장하며, 자동 다음이 켜져 있으면 300ms 뒤 기존 다음 말씀 로직을 실행함
- 가이드 추가는 기존 상태 선택 버튼의 높이·크기 및 `srp:verseStatus` localStorage 저장 구조를 변경하지 않음
- 현재 순서는 카드 상단에 표시함. 이전/다음은 카드 본문 바깥의 30px 화살표와 46px 투명 터치 영역으로 제공함
- 카드에서 왼쪽 스와이프는 다음, 오른쪽 스와이프는 이전으로 이동함. 버튼·입력·링크 등 인터랙티브 요소에서 시작한 제스처는 제외함
- 카드 상단 시작 위치는 세로 중앙 정렬이 아니라 상단 네비게이션 아래 `clamp(60px, 9vh, 84px)`이며 긴 본문은 아래로 늘어나 문서가 스크롤됨
- 본문 글자 크기는 `18 / 20 / 22 / 24 / 26px` 단계로 조절하고 선택값을 저장함

#### 랜덤 암송 및 범위 선택

- 말씀 목록의 `랜덤 암송 ›`은 `랜덤 범위 선택` 바텀시트를 엶
- 범위는 `현재 권 내`, `전체 64구절`, 암송 상태별 세 범위, `말씀구절 직접 선택`으로 구성되며 선택 즉시 기존 큐 생성 로직을 실행함
- 상태별 범위는 저장된 말씀별 암송 상태를 기준으로 전체 말씀에서 필터링함
- 랜덤 큐는 Fisher–Yates 방식으로 섞으며, 선택 결과가 없으면 안내 후 실행하지 않음
- 랜덤 암송 상단에서는 일반 `말씀익히기 | 필사하기` 세그먼트를 숨기고 `‹ 암송 범위`와 `목록으로`를 표시함
- 카드는 처음에 본문을 숨기며 카드를 누르면 전체 정답 본문만 표시함. 랜덤 암송에서는 일반 학습 탭과 글자 크기 조절을 숨김
- 정답 공개 후 암송 상태 footer, 자동 다음, 좌우 화살표와 스와이프 이동을 사용할 수 있음
- `‹ 암송 범위`는 현재 큐를 유지한 채 범위 선택 바텀시트를 다시 열고, `목록으로`는 앱 내부 전환으로 목록과 이전 스크롤 위치로 돌아감

#### 말씀구절 직접 선택

- 직접 선택 화면의 `1권 / 2권 / 3권`은 동일 너비 탭이며 선택 권의 말씀 체크 목록으로 즉시 전환함
- 권 탭은 바텀시트 스크롤 컨테이너 안에서 sticky로 유지되고, 헤더는 고정되지 않음
- 다른 권으로 이동해도 `pendingCustomIds`의 체크 상태와 전체 선택 수가 유지됨
- 권별 전체 선택/해제와 선택 개수 표시가 있으며, 하나 이상 선택했을 때만 `이 범위로 시작` 버튼이 표시됨
- 적용 시 체크된 ID만 섞어 랜덤 암송 큐로 사용함

#### 필사하기

- 필사 상단에는 `‹ 목차`와 `말씀익히기 | 필사하기` 세그먼트가 있고, 말씀 주소 아래에 `따라쓰기 / 암송쓰기` 텍스트 탭이 있음
- 기본 모드는 따라쓰기임. `js/data.js`의 `memorizationChunks`를 사용해 전체 원문을 계속 표시하고 현재 입력할 의미 구간만 강조함
- 현재 구간이 정확하면 기존 입력을 지우지 않고 다음 구간으로 이동하며 커서를 입력 끝에 유지함. 마지막 구간 완료 시 현재 화면에만 `필사를 완료했어요.`를 표시함
- 암송쓰기는 확인 전 원문을 숨기고 전체 말씀을 자유 입력하게 함. `확인하기` 후 실제 작성문에서는 틀리거나 추가된 부분만 표시하고, 누락과 불일치는 아래 원문에서 강조함
- `다시 써보기`는 작성문·비교 결과·원문 표시를 초기화하고 다시 입력받음
- 비교는 한글과 숫자만 사용하므로 문장부호·기호·앞뒤 공백·연속 공백·줄바꿈 차이를 무시함
- textarea는 최소 112px에서 내용에 맞게 자동 증가하고 최대 240px를 넘을 때만 내부 스크롤을 사용함
- 필사 이전/다음은 현재 학습 큐를 재사용하며 말씀 이동 시 필사 시도를 초기화함
- 필사 이전/다음은 남는 세로 공간을 밀어내는 자동 상단 여백 없이 필사 입력 영역 아래에 이어서 표시됨
- 필사 입력, 진행 구간, 완료 여부, 암송쓰기 비교 결과는 저장하지 않음. 필사 화면 이탈, 말씀 이동, 새로고침, 앱 재진입 시 남지 않음

### 3) 현재 UI 및 공통 동작

- 모든 화면 크기에서 같은 모바일 레이아웃을 사용하며 `.screen`과 바텀시트의 최대 너비는 480px임. 넓은 화면에서는 중앙 정렬되고 데스크탑 전용 다단 레이아웃은 없음
- 전체 배경은 장식 없는 단일 아이보리 `#F8F5EE`이며 radial-gradient, noise/paper texture, 라벤더 색면, 곡선 배경을 사용하지 않음
- 카드 배경은 `#FFFDF8`, 주요 강조는 플럼 계열을 사용함
- PWA가 백그라운드로 전환될 때 학습 또는 필사 화면이 열려 있으면 목록 화면으로 전환함. 필사 화면이었다면 메모리상의 필사 상태도 초기화함

### 4) 저장 구조와 건드리면 안 되는 중요 로직

- `js/storage.js`가 사용하는 localStorage 키:
  - `srp:lastVerseId`: 마지막으로 본 말씀
  - `srp:verseStatus`: 말씀별 암송 상태 map
  - `srp:autoAdvance`: 암송 상태 선택 후 자동 이동 설정
  - `srp:verseFontSize`: 학습 본문 글자 크기
- 필사 상태용 저장 키는 없으며 별도 요청 없이 추가하지 말 것
- `js/data.js`의 말씀 ID·순서·`memorizationChunks`, `js/practice.js`의 빈칸 단계, `js/initials.js`의 첫 글자 변환을 UI 수정 과정에서 변경하지 말 것
- `state.mode === "sequential"`과 `state.mode === "random"`에 따라 상단 네비게이션, 카드 공개 방식, 학습 탭 표시, 큐 이동이 달라짐
- 동적 권 탭, 과 헤더, 말씀 버튼, 범위 옵션, 직접 선택 항목의 ID·class·`data-*`·`aria-*`와 이벤트 연결을 함께 확인할 것
- 카드 클릭으로 정답을 공개하는 랜덤 모드에서는 내부 버튼 이벤트의 `stopPropagation()`과 스와이프 제외 대상이 중요함
- 목록 검색 중에는 선택 권을 무시하고 전체 권에서 검색하는 현재 동작을 유지할 것
- `sw.js`의 precache 파일을 변경하거나 정적 파일을 추가할 때는 캐시 목록과 `CACHE_VERSION` 갱신 필요 여부를 함께 검토할 것
- GSAP에 단순히 `defer`를 다시 적용하지 말 것. 앱 모듈의 `initStartHero()`가 GSAP 준비 전에 실행되면 `playHeroIntro()`가 `gsap` 미정의 조건으로 종료되고 `heroStarted`가 이미 설정되어 랜딩 애니메이션이 통째로 재시도되지 않음
- `initStartHero()` 실행 전에 로컬 `window.gsap`이 반드시 준비되는 현재 순서를 유지하고, GSAP을 다시 외부 CDN 의존 구조로 변경하지 말 것
- Pretendard와 Black Han Sans를 다시 외부 폰트 의존 구조로 변경하지 말 것. 폰트 파일 경로나 굵기를 바꾸면 CSS의 `@font-face`, `sw.js` precache와 캐시 버전을 함께 확인할 것
- 랜딩 로딩을 최적화할 때는 `index.html`, `js/startHero.js`, 서비스 워커 precache와 로컬 폰트 구조를 함께 점검하고, 인라인 아이보리 초기 배경을 유지할 것
- 보라색 오버레이·배경 말씀·엠블럼·제목 효과의 기존 초기화 및 실행 순서를 깨뜨리지 말 것

### 5) 현재 미완료 및 테스트 필요 사항

- 코드에서 확인된 미완료 기능이나 중단된 작업은 없음
- UI/UX는 계속 개선 중이므로 현재 구현을 최종 확정 디자인으로 간주하지 말 것
- `index.html`의 PWA 태그, manifest 연결, 서비스워커 등록, 시작 화면 접근성 속성은 현재 모두 존재하며 제거·미완성 상태가 아님
- 전체 JavaScript 파일의 `node --check`와 현재 diff의 `git diff --check`는 통과함
- 자동화된 브라우저/E2E 테스트 구성은 없음. UI 변경 후에는 실제 브라우저에서 다음 흐름을 수동 회귀 확인할 것:
  - 권 탭과 상태 필터 펼침/접힘·복합 필터·전체 권 검색
  - 과 전체/개별 말씀 진입, 네 학습 모드, 상태 저장과 자동 이동
  - 하단 `현재 암송 상태 ⓘ` 팝오버가 레이아웃을 밀지 않고 화면 안에서 열리며 세 기준을 한 줄로 표시하는지, 바깥 클릭과 Escape 키로 닫히는지
  - 카드 화살표·스와이프와 랜덤 카드 공개
  - 범위별 랜덤 큐와 직접 선택 sticky 탭·권 간 선택 유지
  - 따라쓰기 자동 구간 이동과 암송쓰기 비교·재시도·비저장 동작
  - 온라인 상태에서 최신 서비스 워커가 설치되고 `srp-offline-v4` 앱 shell과 로컬 GSAP·폰트가 캐시되는지
  - 앱을 완전히 종료한 뒤 네트워크를 차단하고 PWA를 재실행했을 때 검은 빈 화면이 길게 노출되지 않고 아이보리 초기 배경이 먼저 보이는지
  - 완전 오프라인 재실행에서 보라색 오버레이, 배경 말씀, 엠블럼, 제목 효과가 정상 실행되는지
  - 온라인과 오프라인에서 Pretendard와 Black Han Sans가 동일하게 표시되는지
  - 오프라인 랜딩을 터치한 뒤 목록 및 학습 화면에 정상 진입하는지
  - PWA 설치·오프라인 fallback·백그라운드 복귀 동작
