## HANDOFF — 현재 상태 인수인계

이 문서는 Claude Code와 Codex(또는 다른 협업 에이전트)가 교대로 작업할 때 사용하는 현재 상태 인수인계 문서입니다.
항상 이 파일에는 **현재 작업 트리 기준 상태만** 남기고, 과거 작업 이력은 Git 히스토리에서 확인하세요.

---

### 1) Git 상태

- 브랜치: `main`
- 작업 트리는 clean 상태입니다.
- `main`과 `origin/main`은 동일한 HEAD를 가리킵니다.
- staged, unstaged 또는 untracked 변경은 없습니다.

---

### 2) 현재 구현 상태

- 시작 화면(랜딩/hero), 목차(TOC), 암송 학습 카드, 필사하기, 암송하기 및 랜덤 범위 선택 UI가 구현되어 있습니다.
- `암송하기`의 랜덤 범위 바텀시트는 `범위 선택`, `암송 상태`, `말씀구절 직접 선택`을 구분한 직선형 목록 구조입니다. 각 행 전체가 기존 `data-scope` 로직으로 동작하며 행 사이 구분선과 오른쪽 보조 화살표만 사용합니다.
- 범위 선택으로 시작한 암송(`state.mode === "random"`)에서는 카드 상단의 `말씀익히기 | 필사하기` 세그먼트를 숨기고 `‹ 암송 범위`와 `목록으로`를 표시합니다. `‹ 암송 범위`는 현재 큐를 유지한 채 범위 바텀시트를 다시 열고, `목록으로`는 범위·목차 필터 상태를 유지한 채 앱 내부 전환으로 원래 목차와 스크롤 위치를 복원합니다. 암송 카드를 뒤집으면 최소 말씀 정보와 전체 본문만 표시하며 내부 학습 탭과 글자 크기 조절은 숨깁니다. 카드 footer의 암송 상태 선택과 좌우 이동·스와이프는 유지됩니다.
- 말씀구절 직접 선택 화면의 `1권 / 2권 / 3권`은 동일 너비의 접근 가능한 가로 탭입니다. 진입 시 현재 권(전체 탭에서는 1권) 목록을 즉시 열고, 다른 권을 누르면 빈 중간 상태 없이 해당 목록으로 전환합니다. 기존 체크 선택은 권 전환 및 화면 왕복 후에도 유지됩니다.
- 직접 선택 화면의 권 탭은 바텀시트 목록 스크롤 중 상단에 유지되는 sticky 영역입니다. 불투명 아이보리 배경과 z-index로 목록이 탭 뒤에서 비치지 않으며, 헤더와 닫기 버튼은 기존처럼 함께 스크롤됩니다.
- 암송 연습 모드(`full`, `lineByLine`, `progressive`, `initials`), 암기 상태 선택, 자동 이동, 검색 및 필터링 기능이 유지되어 있습니다.
- 카드 상단의 보조 메뉴는 `필사하기`로 연결되며, 필사 화면의 말씀 주소 아래·실제 필사 영역 위에 배치된 작은 텍스트 탭에서 `따라쓰기 / 암송쓰기`를 선택합니다. 활성 탭만 굵기와 짧고 얇은 밑줄로 구분하며 기본값은 `따라쓰기`입니다.
- 말씀익히기와 필사하기 화면은 동일한 상단 `‹ 목차` 및 동일 크기의 `말씀익히기 | 필사하기` 세그먼트 탭을 사용하며, 현재 말씀을 유지한 채 화면을 전환합니다.
- 모든 화면 크기에서 모바일 UI와 동일한 레이아웃을 사용하며, 공통 `.screen`은 `max-width: 480px`로 넓은 화면의 가로 중앙에 배치됩니다.
- 모든 화면은 기존의 따뜻한 단일 아이보리 배경을 사용하며 radial-gradient, noise/paper texture, 별도 라벤더 색면이나 곡선형 배경 장식은 적용하지 않습니다. 화면의 깊이감은 기존 카드색과 약한 그림자, 타이포그래피 대비로만 구분합니다.
- 폰트 위계는 과·화면 제목 `700~800`, 카드 화면 말씀 주소와 선택·현재 강조 요소 `600~700`, 일반 말씀 본문 및 비활성 컨트롤 `500`, 보조 정보 `400~500`을 기준으로 하며 폰트 종류·크기·색상은 기존 값을 유지합니다. 목록의 요절 주소는 선택 여부와 무관하게 `500`으로 표시하고, 그 아래 암송 상태는 같은 굵기에서 더 옅은 색상·투명도로 한 단계 가볍게 구분합니다. 전체 말씀의 첫 글자 학습 힌트는 기존 대상 로직과 플럼 색상을 유지하고 일반 본문보다 한 단계 굵은 `600`으로 표시합니다.
- 암송 상태 선택은 말씀 카드 안의 별도 박스가 아니라 카드 좌우·하단 경계에 직접 연결되는 footer입니다. 상단 및 세 상태 사이에만 얇은 구분선을 사용하고, 선택 상태의 옅은 라벤더 배경과 기존 저장 로직을 유지합니다.
- 암송 상태 footer는 44px의 최소 터치 높이를 유지하면서 본문과의 위쪽 간격 및 수직 padding을 줄여 컴팩트하게 표시합니다. 글자·아이콘 크기, 선택 배경과 카드 하단 모서리는 유지됩니다.
- 첫 글자 모드의 `확인하기` 버튼은 내용 너비의 compact 버튼으로 표시하며, 진한 외곽선 없이 옅은 라벤더 배경과 미세한 hover/pressed 명도 변화만 사용합니다. 기존 암송 확인 동작은 유지됩니다.
- 말씀익히기 화면은 하단 이전/현재 순서/다음 페이저 대신 말씀 카드 바깥 좌우 여백의 30px 화살표 아이콘과 카드 좌우 스와이프로 이동합니다. 화살표는 시각적 박스 없이 46px의 투명 터치 영역을 사용하고 누르는 순간에만 조금 진해집니다. 현재 순서는 카드 상단 보조 정보에 표시하며, 인터랙티브 요소에서 시작한 스와이프는 무시하고 기존 `goPrev`/`goNext` 및 큐 경계 로직을 재사용합니다.
- 말씀익히기 화면은 viewport 세로 중앙 정렬을 사용하지 않습니다. 말씀 카드는 모든 화면 크기에서 상단 네비게이션 아래 `clamp(60px, 9vh, 84px)` 간격의 동일한 시작 위치에 놓이며, 긴 본문은 아래로 늘어나 자연스럽게 페이지 스크롤을 사용합니다. 하단 자동 이동 영역은 일반 문서 흐름과 추가 하단 여백으로 카드와 겹치지 않습니다.
- 필사 textarea는 모바일에서 3~4줄 정도의 초기 높이로 시작하고 내용에 맞게 자동 확장되며, 최대 높이를 넘을 때만 내부 스크롤을 사용합니다.
- 필사 본문은 항상 전체를 표시하고 `memorizationChunks`를 의미 단위로 재사용해 현재 입력할 구간만 은은하게 강조합니다.
- 현재 구간을 정확히 입력하면 현재 화면의 누적 필사문과 커서를 유지한 채 다음 구간으로 자동 이동합니다.
- 별도 완료 버튼은 없으며 마지막 구간까지 정확히 입력하면 현재 화면에서만 `필사를 완료했어요.` 안내를 표시합니다.
- 필사 비교는 한글·숫자만을 기준으로 하여 공백·줄바꿈·문장부호 및 기호 차이를 무시합니다.
- `암송쓰기`는 확인 전까지 원문을 숨기고 전체 말씀을 자유롭게 입력하게 합니다. `확인하기` 후 `내가 쓴 말씀`에는 실제 입력한 내용만 유지하고 틀리거나 추가된 글자만 은은하게 표시합니다. 누락된 내용은 작성문에 삽입하지 않고, 아래 `원문 전체`에서 틀리거나 빠진 대응 위치를 라벤더 톤으로 강조합니다. `다시 써보기`로 비교 표시와 원문을 다시 숨긴 채 초기화합니다.
- 필사 입력·진행·완료 상태는 저장소에 기록하지 않고 현재 화면에서만 유지하며, 화면 이탈·말씀 이동·새로고침·앱 재진입 시 초기화됩니다.
- 필사 화면의 이전/다음 이동은 현재 말씀 큐와 순서를 재사용합니다.
- 목차 헤더에는 19px 홈 아이콘과 17px·`650` 굵기의 `신천중부교회 · 제자훈련반` 소속 정보를 동일한 크기로 표시하고, 별도 배경 없이 넉넉한 수직 여백으로 브랜드 영역을 구분합니다. 권 선택 줄에는 `전체 / 1권 / 2권 / 3권`만 동일 너비로 배치합니다. 그 아래 검색과 `랜덤 암송 ›`은 하나의 얇은 외곽선·10px radius 컨테이너를 공유하며 내부에는 세로 구분선만 사용합니다. 검색은 남은 너비를, 암송 액션은 최소 너비를 사용하고 기존 검색 및 암송 범위 선택·랜덤 큐 흐름을 유지합니다.
- 목차의 각 과는 모든 화면 크기에서 제목 아래 두 말씀이 2열로 배치됩니다. 라벤더 제목 헤더 전체가 접근 가능한 하나의 버튼으로 동작해 기존 과 전체 학습 흐름으로 진입하고, 오른쪽 화살표는 이동 가능성을 알리는 장식으로만 표시됩니다. 두 개별 말씀 버튼은 각각 독립적으로 작동합니다.
- 목차의 각 과 묶음은 모든 모서리 radius 없이 라벤더 제목 헤더와 두 요절 영역이 직선으로 이어지는 섹션 형태입니다. 별도 외곽선이나 그림자 없이 섹션 사이 12px 간격과 하단 구분선으로 책의 목차처럼 구분합니다.
- 목차 상단의 `전체 / 1권 / 2권 / 3권` 선택 UI는 외곽 캡슐 없이 한 줄의 compact 탭으로 표시합니다. 비활성 탭은 투명하고 활성 탭만 옅은 라벤더 배경과 진한 글씨로 구분하며 기존 권 필터 로직을 유지합니다.
- 목차 검색은 검색 아이콘과 기존 placeholder를 유지하면서 옅은 아이보리 카드색, 1px 테두리와 10px radius를 사용한 compact 입력 영역으로 표시합니다. 래퍼 전체를 눌러 입력할 수 있으며 권 탭→검색→목록 사이 간격은 12px로 통일됩니다.
- 검색창 아래의 `○ 더 익히기 / ◐ 부분 암기 / ✓ 암기 완료` 필터는 모든 권 및 `전체`에서 사용할 수 있는 작은 chip 형태입니다. 권 탭을 누르면 필터가 펼쳐지고 선택된 같은 권을 다시 누르면 권 선택과 현재 상태 필터 값은 유지한 채 UI만 접힙니다. 다른 권을 누르면 해당 권으로 바뀌면서 필터가 펼쳐지며, 같은 상태를 다시 누르면 상태 필터가 해제됩니다.
- 로컬 저장소 기반 상태 관리(`js/storage.js`), 연습 로직(`js/practice.js`), 데이터(`js/data.js`)가 존재합니다.
- PWA 관련 `manifest.json`, `theme-color`, Apple 메타 태그 및 홈 화면 아이콘 연결이 `index.html`에 정상 유지되어 있습니다.
- `index.html`의 서비스워커 등록 스크립트와 `sw.js`가 유지되어 있습니다.
- 시작 화면의 `tabindex="0"`, `role="button"`, `aria-label`을 포함한 키보드 접근성용 DOM 속성이 유지되어 있습니다.
- 랜덤 범위 선택 UI의 `range-option-list`, `custom-picker-screen`, `custom-picker-list` DOM 구조가 관련 `js/app.js` 로직과 호환되는 현재 구조로 유지되어 있습니다.

---

### 3) 미완료 작업 및 검증 상태

- 현재 실제 미완료 작업은 없습니다.
- `js/app.js`, `js/storage.js`는 `node --check`를 통과했고 `git diff --check`에서 문제가 없었습니다.
- Vercel 프로덕션 배포가 완료되었고 공개 URL의 `200 OK` 응답을 확인했습니다.

---

### 4) 작업 주의사항

- 기존에 완료된 기능은 새 요청의 범위에 필요한 경우에만 수정하세요.
- 작업 시작 전 `git status` 및 관련 코드를 다시 확인하세요.
- 이 문서에 과거 작업 이력을 누적하지 말고, 작업 종료 시 현재 실제 상태로 갱신하세요.
