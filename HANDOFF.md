## HANDOFF — 현재 상태 인수인계

이 문서는 Claude Code와 Codex(또는 다른 협업 에이전트)가 교대로 작업할 때 사용하는 최신 상태 인수인계 문서입니다.
항상 이 파일은 **현재 작업 트리 기준으로 갱신**해야 하며, 과거 작업 이력은 Git 히스토리에서 확인하세요.

---

### 1) 현재 상태

- 브랜치: `main`
- 최근 커밋(HEAD): `08027a5` — "Fix service worker serving stale files after every deploy" (작성자: 수진)
- unstaged 변경 파일: `index.html` (working tree에 수정이 있음)
- 최근 커밋에 포함된 변경 파일: `sw.js` (서비스워커 관련 수정)

- 현재 구현된 주요 기능(읽어본 코드 기준):
  - 시작 화면(랜딩/hero), 시작 버튼 및 이어서 학습 버튼 UI
  - 목차(TOC) 화면: 책/과 탭, 검색, 필터링, 목록 렌더링
  - 암송 학습 카드: 다양한 연습 모드(`full`, `lineByLine`, `progressive`, `initials`), 상태 선택, 자동 이동 옵션
  - 랜덤 범위 선택 모달 및 커스텀 선택 UI(구현된 데이터/DOM 구조 존재)
  - 로컬 저장소 기반 상태 관리(`js/storage.js`)
  - 암송 연습 관련 로직(`js/practice.js`) 및 데이터(`js/data.js`)
  - 서비스워커 파일 `sw.js` 존재(최근 커밋에서 캐시 전략 변경 등 수정됨)

---

### 2) 미커밋 변경 상태(요약)

- 대상 파일: `index.html` (unstaged)
- 변경 요약:
  - PWA 관련 태그(`manifest.json`, `theme-color`, `apple-touch-icon`, Apple meta 태그)가 제거됨.
  - 인라인으로 있던 서비스워커 등록 스크립트가 제거됨.
  - 시작 화면 마크업이 정리/수정됨: `tabindex`/`role="button"` 제거, 시작 CTA 버튼(`말씀 암송 시작`)과 이어서 학습 버튼 추가/스타일 변경.
  - 일부 오버레이·스크림 요소가 제거/주석 처리(예: `quote-overlay-scrim` 제거).
  - 목차 헤더 아이콘(원래 SVG) 대신 `kicker-dash` 텍스트로 대체됨.
  - 랜덤 범위 선택 모달 내부 구조가 간소화됨(기존 `range-option-list` wrapper 삭제, `custom-picker-screen` 축소 및 `custom-picker-list`로 교체).
  - 서비스워커 등록 `<script>` 블록이 삭제되어 브라우저에 자동 등록되지 않음.

- 상태: 이 변경은 Claude Code가 작업 도중 중단한 것으로 보입니다. 현재 변경은 **완성 여부가 확인되지 않았음**으로 표기합니다.
- 조치: 현재 상태에서는 해당 변경을 커밋하거나 되돌리지 않습니다(아래 주의사항 참고).

---

### 3) 주의사항

- PWA `manifest` 및 Apple 메타 태그
  - `manifest.json`과 Apple 관련 메타/아이콘은 PWA 설치·아이콘·머티리얼 동작에 영향을 줍니다. 무단 삭제 시 iOS/Android 설치 동작이나 홈스크린 아이콘이 깨질 수 있음.

- Service Worker 등록
  - `index.html`에서 서비스워커 등록 스크립트가 삭제되어 실제 브라우저에서 `sw.js`가 등록되지 않을 수 있습니다. `sw.js`는 최근 커밋에서 변경됨(캐시 전략 등). 등록 동작을 임의로 변경하지 마세요.

- 랜딩(시작) 방식
  - `index.html`의 시작 화면 접근성 속성(`tabindex`, `role`)과 시작 버튼 변경은 사용자 경험, 키보드 접근성, 탭 순서에 영향이 있습니다. 접근성/이벤트 연결(예: 탭/엔터로 시작)이 필요한 경우 기존 구현과 비교 검증 필요합니다.

- 말씀 범위 선택 UI
  - 랜덤 범위 모달 및 커스텀 페이저 관련 DOM 구조 변경은 `js/app.js`의 해당 코드와 연계되어 있으며, DOM 변경으로 인해 이벤트 바인딩이나 기능이 깨질 수 있음.

위 항목들은 기존 커밋(HEAD)과 unstaged 변경이 충돌할 가능성이 있으므로 **임의로 삭제하거나 되돌리지 마십시오**. 변경을 확정하려면 비교·테스트 후 사용자 승인 필요.

---

### 4) 다음 작업(권장 절차)

1. 최근 커밋(HEAD) 기준 코드와 현재 unstaged `index.html`을 비교(diff)하여 각 변경 라인을 분류합니다:
   - `유지`(keep): 변경을 적용해도 문제없고 의도된 변경
   - `되돌림`(revert): 의도치 않은 삭제/변경으로 기능 손상 우려
   - `사용자 확인 필요`(confirm): 디자인/UX 의도가 불명확하여 담당자(수진) 확인 필요

2. 분류 결과를 사용자에게 보고하고, 사용자 승인(또는 구체적 지시)을 받은 후에만 실제 수정(커밋/복원)을 진행합니다.

3. `HANDOFF.md`는 작업 이력을 계속 누적하지 말고 항상 **현재 상태 기준으로 갱신**합니다. 과거 이력은 Git 로그에서 확인하세요.


---

현재 요청대로 이 파일 생성 외에는 기존 소스 코드를 수정하지 마십시오.

<!-- Generated by handoff assistant on current working tree -->
